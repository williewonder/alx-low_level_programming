#include "main.h" 
	#include <stdio.h>
  	/**
 	* _isdigit - check for digits (0-9)
 	* @c : is a digit 
 	* Return: 1 if c is digit, 0 otherwise
 	*/
	int _isdigit(int c)
	{
		if (c >= 0 && c <= 9)
		{	
		return (1);
		}	
		else
		{
		return (0);
		}	

